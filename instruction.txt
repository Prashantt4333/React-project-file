SPA (Single Page Application)

React Is nothing but a Nodejs Project

In order to install a react project you need npm

npm = node Package Manager

-g = globally
 
js = javascript

json = javascript object notation 
npm i npx

npm install -g create-react-app

npx create-react-app  my-react-app



How to change  the default port ( 3000 ) of react project ?

Go to package.json 
	"start":"SET PORT=4000 && react-script-start"

** Ctrl+C is the command to stop the react projest 
 ** y -> Enter **


** REactJs Is a Node JS project

** A react component can contain other react  component 

** What is components ?
	Components are reusable block code USED 
	to genret some ui using html tags(JSX)
	JSX is => Javascript and XML

**	Types of components 
	1. class components 
	   A Class components is created with JS class
	2. Functional components 
	   A functional component is created with JS function






















